/* exported omit */
/*
input: an object & keys which are properties
create a new object
if (keys )

out: a new object with keys that was not matched
*/

// function omit(source, keys) {
//   var notPresentObject = {};
//   var sourceObjectKeys = Object.keys(source); // --> ['foo', 'bar', 'baz']
//   for (var i = 0; i < Object.length + 1; i++) {
//     if (sourceObjectKeys[i] === keys[i]) {
//       sourceObjectKeys.shift();
//     }
//   }
//   console.log(sourceObjectKeys);
//   return notPresentObject;
// }
