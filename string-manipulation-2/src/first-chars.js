/* exported firstChars */

// input: number of length you want to return of a string
// need to stop and cut at the length input
// return the string

function firstChars(length, string) {
  return string.slice(0, length);

}
