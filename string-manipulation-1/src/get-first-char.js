/* eslint-disable no-empty */
/* exported getFirstChar */

/* parameter is a string
need to register that it is a string and get the first letter of the string
output we want is the first character of a string
*/

function getFirstChar(string) {
  if (string === '') {
  }
  return string[0];
}
