/* exported getValue */

/*
input: object & key
need to access the object and the key and value of the object
output: value in the object at the key property */

function getValue(object, key) {
  return object[key];
}
