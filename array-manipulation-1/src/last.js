/* exported last */
/*

input: an array
get only the last index
output: a new array with a last index cut out */

function last(array) {
  var lastIndex = array.length - 1;
  return array[lastIndex];
}
